package main

import "fmt"

func main() { 
	var i, j int = 1, 2  //A var declaration can include initializers.

	k := 3		     // Variable declare with short form
	c, python, java := true, false, "no!"

	fmt.Println(i, j, k, c, python, java)
}
